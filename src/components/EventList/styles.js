import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  listContainer: {
    marginTop: 20,
    padding: 10,
  },
  listTitle: {
    color: 'white',
    marginBottom: 5,
    fontSize: 18,
  },
});

export default styles;
